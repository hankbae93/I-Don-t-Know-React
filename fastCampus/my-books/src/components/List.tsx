import React from "react";

const List = () => {
  return (
    <div>
      <h1>List</h1>
      <button onClick={click}>logout</button>
    </div>
  );

  function click() {}
};

export default List;
